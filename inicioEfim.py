import sys
def iniciar():
    input("Bem vindo a Loki: The Game, aperte espaço para começar!")

def perder(mensagem) :
    input(mensagem)
    sys.exit()

def ganhar(mensagem) :
    input(mensagem)
    sys.exit()
